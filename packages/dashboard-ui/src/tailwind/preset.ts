// Tailwind v4 uses CSS-first config via @theme in globals.css. This file is
// retained as the preset entry point so consumers can `import preset from
// "@flowpunk-indie/dashboard-ui/tailwind-preset"` and pass it to a custom
// Tailwind config when needed (e.g., for v3 interop or future plugin wiring).
// For v4 the canonical configuration lives in `../styles/globals.css`.
export const preset = {
  content: [],
};
export default preset;
